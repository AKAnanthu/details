async function loadRepos(){const box=document.getElementById("githubRepos");try{const r=await fetch("https://api.github.com/users/AKAnanthu/repos?sort=updated&per_page=6");if(!r.ok)throw new Error();const repos=await r.json();box.innerHTML=repos.filter(x=>!x.fork).slice(0,6).map(x=>`<a class="card repo" href="${x.html_url}" target="_blank" rel="noopener"><h3>${x.name}</h3><p>${x.description||"Open-source project and development work."}</p><div class="repo-footer">${x.language||"Repository"} · View on GitHub →</div></a>`).join("")||"<p>No public repositories found.</p>"}catch(e){box.innerHTML='<p style="color:var(--muted)">GitHub projects could not be loaded right now. <a style="color:var(--primary)" href="https://github.com/AKAnanthu" target="_blank">View GitHub →</a></p>"}}loadRepos();